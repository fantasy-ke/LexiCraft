# LexiCraft 架构优化与前后端联调方案

## 2026-08-07 18:41

## 技术方案概述

本方案针对 LexiCraft 当前的多服务后端、YARP 网关、AgileConfig 配置和 Vue 前端并行演进问题，目标是先建立一条可验证的前后端联调链路，再逐步统一 API 契约、测试门禁、配置治理和代码维护边界。

方案遵循现有技术栈，不新增替代性网关、数据库或前端状态框架。第一阶段只解决“请求能否到达正确服务、响应能否被稳定解析、认证能否持续工作”三个问题；路由来源、字段格式和业务规则未确认前，不直接大范围迁移旧 API。

本轮 P0 优化不以配置清理作为前置条件。现有本地局域网地址（包括 `172.31.21.1`）以及当前使用的 AgileConfig `Secret`、连接密码等配置继续保留，不删除、不替换、不迁移；新增代码只读取现有配置，不复制新的凭据。密钥轮换和配置外置属于后续治理事项，不作为本轮联调完成的阻断条件。

## 当前架构事实

| 层次 | 当前实现 | 证据位置 |
| --- | --- | --- |
| 前端 | Vue 3、TypeScript、Vite、Pinia、Vue Router、Axios | `src/UIs/lexicraft-vue-frontend/package.json` |
| 编排 | .NET Aspire Host 注册网关、Identity、Vocabulary、Practice、Files | `src/LexiCraft.Aspire.Host/AppHost.cs` |
| 网关 | YARP、CORS、限流、安全响应头、健康端点 | `src/ApiGateway/LexiCraft.ApiGateway/Program.cs` |
| 服务默认能力 | AgileConfig、服务发现、HttpClient resilience、健康检查、OpenTelemetry | `src/LexiCraft.Aspire.ServiceDefaults/Extensions.cs` |
| Identity | JWT、登录注册、OAuth、用户资料、权限 | `src/microservices/Identity/` |
| Vocabulary | 词库、单词查询、导入、用户单词状态 | `src/microservices/Vocabulary/` |
| Practice | 任务、答案评估、完成任务 | `src/microservices/Practice/` |
| Files | gRPC、内容读取、静态文件、OSS | `src/microservices/Files/` |
| 统一响应 | `ResultDto`，文件流端点为例外 | `src/BuildingBlocks/BuildingBlocks/Shared/ResultDto.cs`、`Filters/ResultEndPointFilter.cs` |

后端代码定义的模块前缀是：

- `api/v{version:apiVersion}/identity`
- `api/v{version:apiVersion}/vocabulary`
- `api/v{version:apiVersion}/practice`

仓库中的网关基础 `appsettings.json` 只保留空的 `ReverseProxy` 节点，但 `appsettings.Development.json` 和 `appsettings.Docker.json` 已提供环境级路由。AgileConfig 或运行环境仍可能覆盖这些配置，因此 P0 以“环境配置来源 + 路由快照 + 实际请求”三项证据确认最终生效值。

当前代码中已确认的路由转换如下，`/api` 是转发到后端服务的内部前缀，不是前端公共地址的一部分：

| 公共入口 | 后端目标路径 | 目标服务 |
| --- | --- | --- |
| `/files/{**catch-all}` | `/{**catch-all}` | `files-grpc` |
| `/identity/{version:regex(^v\d+)}/{**catch-all}` | `/api/{version}/identity/{**catch-all}` | `identity-api` |
| `/vocabulary/{version:regex(^v\d+)}/{**catch-all}` | `/api/{version}/vocabulary/{**catch-all}` | `vocabulary-api` |
| `/practice/{version:regex(^v\d+)}/{**catch-all}` | `/api/{version}/practice/{**catch-all}` | `practice-api` |

## 优化目标与优先级

### P0：建立可验证联调链路（本轮执行范围）

1. **统一网关公共基址**
   - 前端只读取 `VITE_API_BASE_URL`，`IDENTITY_API`、`FILES_API` 由同一个网关基址派生。
   - 本地默认值仅用于开发启动：`http://localhost:5000`；当运行环境使用 `172.31.21.1` 等本地局域网地址时，保留现有地址和端口，不自动改写为 `localhost`。
   - 禁止新增 `localhost:5001`、`localhost:5002` 等服务直连兜底；旧业务 API 的路径迁移留在 P1，避免本轮扩大范围。

2. **固定并验证 YARP 路由契约**
   - 以环境实际加载的 `ReverseProxy` 为准，输出只包含 route、cluster、destination、transform 的脱敏快照，不把 Secret 或密码写入快照。
   - 固定四条公共入口及路径转换：`identity`、`vocabulary`、`practice` 使用 `/v1` URL 版本段，`files` 保持无版本入口。
   - 至少验证验证码、登录、用户资料和一个文件内容请求分别命中预期服务；404、权限失败和服务不可达必须保留真实 HTTP 状态。

3. **统一认证和响应边界**
   - 认证请求统一使用 `Authorization: Bearer <token>`；登录、注册、刷新、OAuth、密码找回等匿名端点不携带失效 Token，也不触发刷新循环。
   - 后端当前线上的 `ResultDto`/`TokenResponse` 采用 PascalCase（`Status`、`Data`、`StatusCode`、`Token`、`RefreshToken`）；前端在 HTTP 边界一次性归一化，业务层不再分散兼容判断。
   - 刷新令牌沿用后端当前轮换语义；正常请求只在令牌临近过期时刷新，收到 401 时强制刷新一次，并发请求共享同一个刷新 Promise，失败后清理会话。
   - 后端当前未返回 `expiresIn`，客户端优先读取 JWT 的 `exp`；不能把 `expiresIn` 当作本轮必须新增的后端字段。

4. **保留现有联调配置，不把清理工作混入 P0**
   - `172.31.21.1` 作为本地局域网地址保留；现有 AgileConfig `Secret`、数据库/缓存/对象存储密码等也按当前部署方式保留。
   - 本轮不删除、不轮换、不替换这些值，也不因为它们仍在现有配置中而阻塞登录链路验证；后续如需安全治理，单独建立迁移和轮换任务。

5. **完成最小主链路冒烟**
   - 网关健康检查 → 验证码 → 注册/登录 → 获取用户资料 → 令牌过期刷新 → 登出。
   - 每一步记录请求公共地址、实际路由、HTTP 状态、响应包络和追踪 ID；没有真实运行环境时只能标记为“静态/构建验证”，不能写成运行时通过。

### P0 完成定义

- 前端认证请求只经由一个可配置网关基址，仓库中不存在认证客户端的服务直连 fallback。
- `Status`/`Data`/`Token` 等实际响应能够被前端稳定解析为内部统一的 `ResultDto` 和 Token 结构。
- 同一失效令牌触发的并发 401 只产生一次刷新请求，原请求最多重放一次。
- 路由快照与实际请求结果能够证明 Identity、Vocabulary、Practice、Files 的路径转换没有漂移。
- 现有局域网地址、Secret 和密码配置保持不变；它们不是本轮 P0 的删除对象。

### 本轮已落地的 P0 代码调整

- `src/UIs/lexicraft-vue-frontend/src/config/env.ts`：增加 `VITE_API_BASE_URL`，由统一网关基址派生 Identity/Files 地址。
- `src/UIs/lexicraft-vue-frontend/src/utils/authHttp.ts`：移除 Identity 直连 fallback，兼容 PascalCase/camelCase 的 `ResultDto` 和 TokenResponse，补齐 429 错误映射，并在 401 时强制刷新。
- `src/UIs/lexicraft-vue-frontend/src/utils/tokenManager.ts`：支持 401 强制刷新，同时保留并发刷新锁。
- `src/UIs/lexicraft-vue-frontend/src/types/auth.ts`、`src/UIs/lexicraft-vue-frontend/src/config/auth.config.ts`：补充限流错误类型和用户提示。
- 未修改网关、Compose、AgileConfig、Secret、密码及局域网地址配置。

### P1：补齐业务契约和工程门禁

1. 建立 `identity`、`vocabulary`、`practice`、`files` 四个前端 API 域模块。
2. 用 OpenAPI 或共享契约生成/校验 TypeScript DTO。
3. 增加网关、Identity、Vocabulary、Practice、Files 的最小契约测试。
4. 让 Vitest 测试脚本真实可执行，并将类型检查/构建纳入 CI。
5. 统一 `src/LexiCraft.slnx` 与根 `LexiCraft.sln` 的项目职责。

### P2：降低长期维护成本

1. 拆分已超过 800 行的后端和前端活跃文件。
2. 明确生产健康探针、CORS、CSP、限流和 OpenTelemetry 导出策略。
3. 补充事件版本、幂等、重试、补偿和跨服务一致性文档。
4. 清理 README、Docker 部署文档与实际项目清单之间的漂移。

## 模块划分与职责

| 模块 | 职责 | 本阶段动作 |
| --- | --- | --- |
| 网关契约层 | 公共入口、路由转换、限流、安全头、CORS | 输出脱敏路由快照；验证路径和错误响应 |
| Identity API 客户端 | 登录、注册、验证码、资料、头像、OAuth、令牌 | 收敛基址和 TokenResponse；覆盖 401 刷新 |
| Vocabulary API 客户端 | 词库、搜索、分页取词、状态、薄弱词 | 替换旧 `word/*`、`dict/*` 路径前先完成映射 |
| Practice API 客户端 | 创建任务、提交答案、完成任务 | 新增类型化模块和状态处理 |
| Files API 客户端 | 上传、内容读取、访问 URL | 明确文件大小、类型、权限和路径契约 |
| 共享契约 | DTO、错误码、权限码、版本 | 以 OpenAPI/服务端 DTO 为单一来源 |
| 测试与门禁 | 单元、契约、网关冒烟、构建 | 修复测试脚本和既有基线阻塞 |
| 配置治理 | 环境变量、AgileConfig、现有本地配置 | 验证加载来源；本轮保留现有局域网地址、Secret 和密码 |

## 前后端统一契约设计

### 1. 公共地址

前端只配置一个网关公共基址：`VITE_API_BASE_URL`。代码中的服务域只保存相对路径，不保存 Identity、Vocabulary、Practice 或 Files 的内部端口。

- 默认本地开发基址为 `http://localhost:5000`，仅作为没有注入环境变量时的开发 fallback。
- 使用局域网联调时，直接把现有的 `172.31.21.1` 和端口注入 `VITE_API_BASE_URL`，不替换、不删除，也不把该地址当成公网地址。
- 认证请求形如 `${VITE_API_BASE_URL}/identity/v1/login`；文件请求形如 `${VITE_API_BASE_URL}/files/content`。
- 后端目标路径中的 `/api/v1/...` 由 YARP transform 生成，前端不重复拼接 `/api`。
- `appsettings.Development.json` 与 `appsettings.Docker.json` 的路由是当前代码事实；AgileConfig/运行环境覆盖后的最终结果仍需在目标环境实测确认。

### 2. 响应包络

后端 `ConfigureJson()` 当前关闭了默认命名策略，线上 JSON 字段以 PascalCase 为准；`ResultEndPointFilter` 会把普通端点结果包装为 `ResultDto`，文件流例外。典型成功响应为：

```json
{
  "Status": true,
  "Message": "",
  "StatusCode": 200,
  "Data": {}
}
```

前端在 `authHttp` 边界统一归一化为内部结构：

```ts
{
  status: true,
  message: '',
  statusCode: 200,
  data: {}
}
```

失败响应必须保留以下信息：

- HTTP 状态：400、401、403、404、409、429、5xx。
- 包络中的 `Status`/`Message`/`StatusCode`/`Data`（若服务返回包络）。
- `Retry-After`、追踪 ID 或其他诊断头（若网关/服务提供）。
- 文件流按 `Content-Type` 处理，不强制包装成 JSON。

前端不得把 HTTP 错误静默转换成成功结果，也不得因为字段大小写差异重新实现一套业务错误判断。

### 3. 认证契约

后端当前 `TokenResponse` 的真实字段为 `Token` 和 `RefreshToken`，不包含 `expiresIn`；前端 HTTP 边界将其归一化为业务层使用的 `token` 和 `refreshToken`，不再依赖大小写猜测。

- 请求头统一为：`Authorization: Bearer <token>`。
- 登录、注册、刷新和 OAuth 回调的返回字段使用归一化后的 Token 结构；后端不因本轮前端适配被迫新增 `accessToken` 或 `expiresIn`。
- 令牌有效期优先从 JWT `exp` 计算；本地状态的兜底时间不作为服务端契约。
- 后端刷新实现会删除旧刷新令牌并生成新的刷新令牌，本轮沿用该轮换语义。
- 普通请求只在访问令牌临近过期时刷新；收到 401 时强制刷新一次，并发请求共享同一个刷新 Promise。
- 登录/注册/刷新/OAuth/密码找回等匿名端点不得携带失效令牌导致刷新循环。
- 注册密码校验仍以服务端规则为准；现有密码配置和值保留，本轮只验证前后端规则差异，不删除密码字段或密码配置。

### 4. 权限契约

后端端点使用权限声明；后端需提供权限码清单和端点映射。前端统一处理：

- 401：尝试一次刷新，失败后清理会话并跳转登录。
- 403：显示无权限状态，不误判为未登录。
- 429：显示限流提示并遵守 `Retry-After`（若提供）。
- 业务操作按钮根据权限状态隐藏、禁用或在服务端拒绝后给出明确反馈。

## API 对接清单

### Identity

| 方法 | 代码相对路径 | 前端动作 |
| --- | --- | --- |
| `POST` | `login` | 对齐登录请求和 TokenResponse |
| `POST` | `register` | 对齐验证码字段和密码规则 |
| `GET` | `users/captcha` | 图片 Base64 与 captchaKey 类型化 |
| `POST` | `refresh-token` | 统一刷新返回和并发锁 |
| `POST` | `logout` | 明确是否需要 Bearer、服务端失效语义 |
| `GET/PUT` | `users/info` | 对齐 `UserId/UserName/Avatar` 字段 |
| `POST` | `uploadAvatar` | 对齐 multipart 字段、大小和返回 URL |
| `GET/POST` | `oauth/{provider}/initiate|callback` | 对齐 state、code、redirectUri 和回调域名 |
| `GET` | `permissions/{userId}` | 对齐权限列表和权限码来源 |

`auth.ts` 中的邮箱验证、密码重置、OAuth 绑定、会话查询和撤销会话方法，当前扫描到的 Identity 端点未全部对应；后端需明确它们是未实现、外部服务还是旧版本遗留。

### Vocabulary

| 方法 | 代码相对路径 | 前端动作 |
| --- | --- | --- |
| `GET` | `words?keyword=` | 替换旧搜索封装，确认空关键词和分页语义 |
| `GET` | `word-lists?category=` | 建立词库列表类型 |
| `GET` | `word-lists/{id}/words?pageIndex=&pageSize=&seed=` | 固定分页从 0/1 开始、seed 和总数结构 |
| `POST` | `word-lists/import` | 确认 JSON 请求体、大小和导入结果 |
| `POST` | `user-words/state` | 对齐 `UserId/WordId/State/MasteryScore` |
| `GET` | `user-words/weak?userId=` | 确认 userId 是否应从 JWT 推导而不是前端传入 |

### Practice

| 方法 | 代码相对路径 | 前端动作 |
| --- | --- | --- |
| `POST` | `tasks` | 明确任务创建请求和任务 ID |
| `POST` | `assessments/submit` | 对齐答案、评估结果和幂等键 |
| `PUT` | `tasks/{taskId}/complete` | 对齐完成状态和重复提交行为 |

### Files

| 方法 | 代码相对路径 | 前端动作 |
| --- | --- | --- |
| `GET` | `content?relativePath=` | 禁止任意路径；确认访问令牌和返回 Content-Type |
| `POST`/gRPC | 文件服务上传接口 | 确认上传协议、大小、类型、进度和权限 |
| 静态资源 | `/uploads` | 明确是否公开、签名 URL 或必须带鉴权 |

## 配置与部署设计

1. 保留当前各环境的配置加载方式；P0 只验证配置来源和最终生效值，不为了“安全清理”删除已有联调配置。
2. `172.31.21.1` 作为本地局域网地址保留；现有 AgileConfig `Secret`、数据库/缓存/OSS 密码等继续按当前方式提供，不能被本轮优化自动替换为占位符。
3. YARP 路由建立只包含 route、cluster、destination、transform 的脱敏快照；快照不记录 Secret、密码、连接字符串或令牌。
4. 前端通过 `VITE_API_BASE_URL` 注入网关基址；Identity/Files 地址从该基址派生，代码中不再保留服务直连 fallback。
5. 生产 CORS 只允许明确的前端域名；同步配置 OAuth 回调、文件域名和 CSP 资源来源。该项属于运行环境验收，不要求本轮删除本地配置。
6. 后续若要做凭据外置、轮换或权限收敛，单独建立配置治理任务；不得把该任务与 P0 登录主链路混合验收。

## 测试与验收设计

### P0 验收

- Given 网关使用当前环境配置启动，When 访问健康检查和路由探针，Then 能证明公共入口与目标服务均可达，且路由转换与快照一致。
- Given `VITE_API_BASE_URL` 指向本机或 `172.31.21.1` 的网关地址，When 请求验证码，Then 前端得到统一的 `captchaData` 和 `captchaKey`，请求不直连 Identity 服务端口。
- Given 合法注册或登录数据，When 调用 `/identity/v1/register` 或 `/identity/v1/login`，Then `Status`/`Data`/`Token`/`RefreshToken` 能被前端归一化，且能建立会话。
- Given 有效访问令牌，When 请求用户资料，Then 使用 `Authorization: Bearer` 经网关路由到 Identity，并返回统一包络。
- Given 访问令牌过期或服务返回 401 且刷新令牌有效，When 多个请求同时失败，Then 只发生一次刷新，刷新后的原请求最多重放一次。
- Given 路由、权限、参数、限流或服务错误，When 请求失败，Then 前端能区分 400/401/403/404/409/429/5xx，不把失败包装成成功。
- Given 当前环境仍使用 `172.31.21.1`、Secret 或密码，When 完成 P0 验收，Then 这些配置值保持原样，且未因本轮优化被删除或替换。

> 运行记录必须区分“静态代码检查/构建通过”和“真实网关联调通过”。没有启动网关、Identity、Redis/数据库和配置中心时，不得把 P0 运行时验收标记为通过。

### P1 验收

- Vocabulary、Practice、Files 客户端均有类型化请求、响应和错误处理。
- 前端 Vitest 命令真实运行，至少覆盖认证客户端、TokenManager 和错误映射。
- 后端有网关/服务最小集成或契约测试，能验证路径、权限和 ResultDto。
- `dotnet build src\\LexiCraft.slnx` 与前端 `build-tsc` 的历史阻塞项被单独修复或在 CI 中明确隔离。

### P2 验收

- 代码行数技术债有拆分计划，拆分后行为测试通过。
- 生产探针、CORS、CSP、限流和 OpenTelemetry 有环境级验证记录。
- README、compose、Aspire 和 OpenAPI/联调文档的服务清单一致。

## 实施顺序

1. 固定当前环境的网关地址、路由来源和配置加载方式；保留 `172.31.21.1`、Secret、密码等现有联调值。
2. 前端统一读取 `VITE_API_BASE_URL`，先完成 Identity 主链路的 `ResultDto`/Token 归一化和 401 刷新验证。
3. 使用网关实际配置完成健康检查、验证码、登录、用户资料、刷新和登出冒烟；记录路由与 HTTP 证据。
4. P0 运行链路稳定后，再接 Vocabulary、Practice、Files 类型化 API。
5. 补契约测试、修复测试脚本和 CI 门禁。
6. 最后处理配置外置/轮换、解决方案漂移、大文件拆分和文档清理等非 P0 事项。

## 待确认项

- AgileConfig 中实际生效的 YARP routes/clusters、path transform 以及是否覆盖 `Development`/`Docker` 文件配置。
- 各环境网关公共地址、TLS、CORS 来源和 OAuth 回调地址；特别确认 `172.31.21.1` 的端口和可达范围。
- 实际错误响应是否始终包含 `Status`/`Message`/`StatusCode`，以及异常处理中追踪 ID 的字段/响应头名称。
- `TokenResponse` 的 JWT `exp`、刷新令牌轮换失效时间和登出后的服务端失效语义。
- 前后端密码校验规则差异是否需要后续收敛；本轮不删除现有密码配置。
- `auth.ts` 中未扫描到后端实现的认证方法归属。
- Practice 请求/响应 DTO、幂等规则和文件上传协议。

---
