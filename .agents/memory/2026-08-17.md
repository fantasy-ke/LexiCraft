# 2026-08-17 幂等基础组件注释与文档

## 需求
为 `BuildingBlocks.Idempotency` 幂等处理基础组件补充 XML 文档注释（接口、类、记录类型、方法、主流程调用链），并新增 README 与可编译使用示例。范围限定纯文档：不改运行逻辑、不新增配置能力、不顺手接入业务端点。

## 代码事实
- 组件公开 API：`IIdempotencyStore`（接口 + `IdempotencyAcquireStatus` 枚举 + `IdempotencyLease`/`IdempotencyStoredResponse`/`IdempotencyAcquireResult` 记录）、`IdempotentAttribute`、`IdempotencyMode`（Replay/Reject/Lock）、`IdempotencyOptions`、`IdempotencyExtensions`（`AddIdempotency`/`UseIdempotency`）、`IdempotencyMiddleware`。
- 主流程在 `IdempotencyMiddleware.InvokeAsync`：读元数据 → 读键 → 算指纹 → `TryAcquireAsync` → 分支（Acquired/Completed/InProgress/FingerprintMismatch）。
- 默认存储 `RedisIdempotencyStore` 依赖 `BuildingBlocks.Caching` 的 `IRedisConnectionFactory`；`AddIdempotency` 要求先 `AddCaching` 或先注册自定义 `IIdempotencyStore`。
- 存储键按用户作用域隔离（已认证用户标识或 `anonymous`）；指纹含 方法/PathBase/Path/QueryString/ContentType/请求体。

## 实现/文档变更
- 用 Node 脚本为 4 个此前缺注释的文件精确插入 `///` 注释（仅新增、0 逻辑改动、保持 LF）：`IdempotencyMiddleware.cs`（+95 行，主流程与私有方法/记录）、`Internal/RedisIdempotencyStore.cs`（+83）、`Internal/IdempotencyRequestFingerprint.cs`（+18）、`Internal/BoundedResponseBufferStream.cs`（+83）。合计 279 行。
- 此前已由用户补齐注释的 3 个文件（`IIdempotencyStore.cs`、`IdempotentAttribute.cs`、`IdempotencyOptions.cs`）及已有注释的 `IdempotencyMode.cs`、`IdempotencyExtensions.cs` 本次保留未动。
- 新增 `src/BuildingBlocks/BuildingBlocks.Idempotency/README.md`：介绍、核心概念、依赖与接入、配置表、Minimal API / MVC 案例、主流程、模式对比、状态码速查、范围与测试。

## 涉及文件
- 修改（本次）：`IdempotencyMiddleware.cs`、`Internal/RedisIdempotencyStore.cs`、`Internal/IdempotencyRequestFingerprint.cs`、`Internal/BoundedResponseBufferStream.cs`
- 新增（本次）：`README.md`
- 既存未提交（同任务、一并提交）：`Abstractions/IIdempotencyStore.cs`、`IdempotentAttribute.cs`、`Options/IdempotencyOptions.cs`
- 未提交且与本任务无关（保持不动）：`src/LexiCraft.sln.DotSettings.user`、`src/microservices/Files/LexiCraft.Files.Grpc/fliesdb*` 运行文件

## 验证结果
- `dotnet build` 组件项目与 Tests 项目：0 警告 0 错误。
- `dotnet test` 幂等组件测试：12/12 通过。
- `git diff --check` 通过（仅预存 3 文件在 `core.autocrlf=true` 下的 LF→CRLF 提示，非本次引入）。
- 注：工作副本为 LF；`core.autocrlf=true` 会在下次 `git add` 时提示 LF→CRLF，属仓库既有 autocrlf 行为，不影响 `diff --check` 与构建。

## 已知限制
- 组件尚未接入任何业务服务；README 未声称 Identity/Vocabulary/Practice 已启用幂等。
- 注释为纯文档，未改变公开契约与行为。

## 待办
- 后续若接入业务端点：在目标端点声明 `IdempotentAttribute`、确认网关/前端约定 `Idempotency-Key` 头、在真实 Aspire/部署环境做运行时验收（Redis 多副本、并发重放、Retry-After）。

## Git 提交信息
`docs: 补充幂等基础组件 XML 注释与 README`
