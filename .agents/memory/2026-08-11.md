# 2026-08-11

## 进入页面白屏排查

### 需求

- 检查前端进入页面后出现白屏的问题。

### 代码事实

- 本地 Vite 开发服务 `http://localhost:3000/` 可以完成 Vue 挂载：首页 DOM、登录页 DOM 均正常生成，控制台未出现项目运行时异常。
- 访问受保护的 `/app` 会按预期跳转到 `/login?redirect=/app`。
- `public/service-worker.js` 未实现资源缓存或请求拦截缓存，不存在由当前 Service Worker 返回旧前端资源的代码路径。
- 先前提供的 `dynamic-i18n version mismatch: expected 6, got 5` 来自 `moz-extension://.../content_main.js`，属于沉浸式翻译浏览器扩展脚本，不是 LexiCraft 构建产物。
- 当前未复现由 LexiCraft 代码触发的白屏，因此未添加覆盖异常的 CSS 或推测性初始化逻辑。

### 实现与文件

- 未修改业务代码。
- 新增本日排查记录：`.agents/memory/2026-08-11.md`。

### 验证结果

- `npm --prefix src\UIs\lexicraft-vue-frontend run build`：通过，Vite 生产构建成功；仅有既有动态/静态导入和大分块警告。
- 本地开发页面真实检查：首页与登录页正常挂载，无应用错误日志。
- 生产预览服务器返回 LexiCraft 的 `index.html`，HTTP 状态为 200。
- `npm --prefix src\UIs\lexicraft-vue-frontend run build-tsc`：失败，均为既有 TypeScript 类型问题与缺少 `vitest` 类型，未涉及本次记录文件。

### 已知限制与待办

- 内置浏览器的可视区域当前不可见，且预览端口的页面快照被其他本地 PWA 状态污染，不能作为生产预览视觉证明；以开发页真实 DOM、控制台和生产 HTTP/构建结果为准。
- 若用户浏览器仍白屏，优先禁用或升级沉浸式翻译扩展、完全重启浏览器并清理 localhost 站点数据；若问题仍存在，需要采集禁用扩展后的第一条红色控制台错误和具体访问 URL。

### Git 提交

- `docs: 记录前端白屏排查结果`（本次任务）。
## Aspire 健康探针取消误报修复

### 需求

- 修复 Aspire 编排启动时 `/health` 偶发 `TaskCanceledException`，被 Serilog 记录为 500 错误的问题。

### 代码事实

- `AddDefaultHealthChecks()` 当前只注册同步的 `self` 检查，`/health` 没有数据库或消息队列等慢速健康检查。
- `HealthCheckMiddleware` 将 `HttpContext.RequestAborted` 传给 `DefaultHealthCheckService.CheckHealthAsync`；Aspire 健康检查调用方在资源状态切换、停止或放弃请求时会取消该令牌。
- 日志中的 `TaskCanceledException` 因此属于调用方取消，不是服务内部健康检查执行超时；增加服务端延迟会延长请求并增加被取消概率。
- Aspire 13.4.6 的 `WithHttpHealthCheck` 没有公开初始延迟或请求超时参数；`WithHttpProbe` 的探针参数不会替代本地 AppHost 立即执行的 HTTP 健康检查。

### 实现与文件

- 修改 `src/BuildingBlocks/BuildingBlocks.SerilogLogging/Utils/SerilogRequestUtility.cs`。
- 当且仅当请求路径为 `/health` 或 `/alive`、异常属于 `OperationCanceledException` 且 `RequestAborted` 已取消时，将请求日志降为 `Debug`。
- 普通业务请求取消、健康端点真实异常和真实 5xx 仍保持 `Error`，没有吞异常或修改健康状态判断。

### 验证结果

- `dotnet build src\BuildingBlocks\BuildingBlocks.SerilogLogging\BuildingBlocks.SerilogLogging.csproj --no-restore`：通过，0 个错误；仅有既有 NuGet 多源警告。
- `dotnet build src\LexiCraft.slnx --no-restore`：通过，0 个错误；75 个既有 NuGet 包源和 Humanizer 版本约束警告。
- 临时行为验证通过：已取消的 `/health` 返回 `Debug`；已取消的业务接口仍返回 `Error`；健康端点真实异常仍返回 `Error`。
- `git diff --check`：提交前执行。

### 已知限制与待办

- 本次未启动完整 Aspire 编排，避免触发远程数据库迁移、种子数据和用户当前服务环境；运行时结论由实际调用链、完整构建和日志分级行为验证支持。
- 重新启动 Aspire 后，正常健康检查仍可保留 Information 日志；调用方取消的健康检查不会再以 `[ERR]` 输出。若资源实际不健康，Aspire 状态和真实异常日志仍会正常暴露。

### Git 提交

- `fix: 避免健康探针取消误报错误`（本次任务）。

## 前端 Zen 主题刷新白屏修复

### 需求

- 重新检查 `65c6bfc docs: 记录前端白屏排查结果` 未解决的问题：前端运行后刷新页面无报错、无异常，但整个页面为空白。
- 对比 2026 年 8 月 10 日的前端改动，确认具体引入提交并完成修复。

### 代码事实

- `65c6bfc` 仅新增 `.agents/memory/2026-08-11.md`，没有修改前端业务代码，因此该提交只记录了排查结论，没有修复白屏。
- 本地开发页可稳定复现：Vue 已完整生成首页 DOM，控制台没有错误，但浏览器截图为纯黑画面。
- 运行时样式显示 `<html>` 的计算样式为 `display: none`，所有页面节点的布局尺寸均为 0。
- 错误规则来自 `src/UIs/lexicraft-vue-frontend/src/layout/modern/LayoutHeader.vue`。提交 `a3f59bc` 在替换 `BrandLogo` 后将完整 `:global(...)` 选择器改成了 `:global(html[...]) .corner-brand :deep(...)` 形式；Vue scoped CSS 实际编译出 `html[data-theme-style="zen"] { display: none; }`。
- 用户此前选择 Zen 主题后，`data-theme-style="zen"` 会在刷新初始化时重新应用，因此刷新立即隐藏整个 `<html>`；这也解释了为什么 DOM 存在且控制台没有异常。

### 实现与涉及文件

- 修改 `src/UIs/lexicraft-vue-frontend/src/layout/modern/LayoutHeader.vue`。
- 将 Zen 主题 Logo 副标题、菜单图标和 Ink 主题 Logo 图形的三个错误 scoped/global 组合选择器改为完整的全局后代选择器。
- 修复后编译选择器只命中 `.brand-logo__copy small`、`.note-links svg` 和 `.brand-logo__mark`，不再直接命中 `<html>`。

### 验证结果

- 当前保存的 Zen 主题下，开发首页从纯黑画面恢复为暖调书卷首页；`<html>` 为 `display: block`，首页根节点高度为 4441.75px，控制台无 error。
- 浏览器刷新 `http://localhost:3000/` 后仍正常显示，标题和正文完整，控制台无 error。
- 直接访问 `http://localhost:3000/app` 正确跳转到 `/login?redirect=/app`；登录页可见，刷新后仍可见且无 error。
- 三个修复后的运行时 CSS 规则均带目标后代选择器，没有 `html[data-theme-style="zen"] { display: none; }`。
- `npm --prefix src\UIs\lexicraft-vue-frontend run build`：通过；仅有既有动态/静态导入和大 chunk 警告。
- 生产构建 CSS 检查通过：不存在隐藏 Zen 主题根 `<html>` 的 `display: none` 规则。
- `npm --prefix src\UIs\lexicraft-vue-frontend run build-tsc`：失败；仍为既有 `QuestionForm.vue`、基础组件、列表、文章/用户页面类型问题及缺少 `vitest` 类型，未包含本次 `LayoutHeader.vue`。
- `git diff --check`：提交前执行。

### 已知限制与待办

- 未使用真实账号进入已登录学习空间；已通过保存的 Zen 主题、公共首页、认证重定向登录页及实际浏览器刷新验证根节点不再被隐藏。
- 仓库其他页面仍存在少量 `:global(html[...]) .local` 写法，但当前仅使用 `transform: none`，未导致根节点隐藏；本次保持最小范围，不做无关主题样式重构。

### Git 提交

- `fix: 修复 Zen 主题刷新白屏`（本次任务）。
## 首页与学习页面视觉收敛

### 需求

- 参考 OpenChamber 的克制留白与清晰信息层级，再次优化公共首页。
- 同步调整主题选择、文章页和词汇页，减少花哨装饰，明确主要按钮和学习路径。

### 代码事实

- 公共首页、文章页和词汇页原有大量渐变、悬浮位移、旋转卡片和装饰性元素，主要操作与辅助操作的视觉权重接近。
- 文章页与词汇页的数据加载、练习入口、书籍/词典管理逻辑可保留，仅需重组模板与样式；两个页面的 `<script>` 区块在本次修改前后保持一致。
- `Book.vue` 的新增卡片原来固定显示“添加词典”，无法在文章页表达“选择书籍/添加书籍”。
- `components.d.ts` 由 Vite 自动组件插件根据模板使用情况更新，移除了本次页面不再使用的图标声明。

### 实现与涉及文件

- 重写 `src/UIs/lexicraft-vue-frontend/src/pages/home.vue`：采用暖色纸张背景、清晰两栏首屏、今日学习示例、三步学习流程和克制的功能说明。
- 重组 `src/UIs/lexicraft-vue-frontend/src/pages/(words)/words.vue`：突出当前词典、今日任务、主练习入口和自由练习次级入口。
- 重组 `src/UIs/lexicraft-vue-frontend/src/pages/(articles)/articles.vue`：突出当前书籍、本周记录、阅读进度和继续阅读入口。
- 简化 `src/UIs/lexicraft-vue-frontend/src/assets/css/themes.scss`、`main.scss`、`BaseButton.vue` 和 `Book.vue`：取消卡片旋转、悬浮抬升、强渐变和不规则按钮；保留三套低干扰主题及深色模式。
- 修改 `src/UIs/lexicraft-vue-frontend/src/layout/modern/LayoutHeader.vue`：主题选项改为“暖调阅读 / 极简专注 / 自然笔记”，并增加用途说明和明确的浅色、深色、跟随系统选项。
- `Book.vue` 新增 `addText` 属性，文章页显示“选择书籍/添加书籍”，词汇页保持“添加词典”。

### 验证结果

- `npm --prefix src\UIs\lexicraft-vue-frontend run build`：通过；仅保留既有动态/静态导入与大 chunk 警告。
- `npm --prefix src\UIs\lexicraft-vue-frontend run build-tsc`：失败；错误仍来自既有 `QuestionForm.vue`、基础组件、列表、其他文章/用户页面及缺少 `vitest` 类型，本次改动文件未出现在错误列表中。
- `git diff --check`：本次前端文件通过。
- 本地真实界面核验：公共首页桌面端和 390×844 移动端均可正常显示；文章页、词汇页首屏层级清晰；主题菜单可打开，三种界面风格可实际切换。
- 为检查受保护页面，仅临时关闭本地路由认证标记；检查结束后已按原始文件恢复，`router.ts` 无差异。

### 已知限制与待办

- 文章和词汇页面在无真实账号数据的本地状态下完成视觉核验，未执行真实后端数据联调或完整学习流程。
- 前端仍有既有 TypeScript 错误和主 chunk 体积警告，本次视觉任务未扩大范围处理。

### Git 提交

- `feat: 收敛学习页面视觉与操作层级`（本次任务）。

## 注册页与登录页视觉统一

### 需求

- 注册页面同步当前登录页面的风格，解决两侧视觉语言、布局和按钮层级不一致的问题。

### 代码事实

- 登录页已经采用“语言学习期刊”式双栏结构、纸张纹理、编辑式大标题和清晰的表单卡片；注册页仍是绿色渐变宣传区、插画占位符和通用 Tailwind 表单，风格明显割裂。
- 注册接口、验证码、表单字段和提交逻辑无需重做，本次仅调整页面结构、文案和视觉样式。
- `VALIDATION_RULES` 使用只读规则数组，注册页直接传递会触发 `FormRules` 的只读/可变数组类型错误。

### 实现与涉及文件

- 重写 `src/UIs/lexicraft-vue-frontend/src/pages/(user)/register.vue` 的模板和样式，复用登录页的品牌页眉、左右双栏、编辑式标题、表单卡片、主按钮和页脚语言。
- 保留邮箱、用户名、密码、确认密码、验证码、注册接口和校验流程；补充返回首页操作与输入框自动填充属性。
- 使用展开语法复制注册校验规则数组，消除该页面的 `FormRules` 只读数组类型错误。
- 桌面端保留双栏信息层级；移动端改为纵向排版，并确保验证码、提交按钮、登录入口和页脚可滚动访问。

### 验证结果

- `npm --prefix src\UIs\lexicraft-vue-frontend run build`：通过；仅有既有动态/静态导入和大 chunk 警告。
- `npm --prefix src\UIs\lexicraft-vue-frontend run build-tsc`：失败；注册页已不在错误列表中，剩余错误来自既有 `QuestionForm.vue`、基础组件、列表、文章/会员页面及缺少 `vitest` 类型。
- `git diff --check`：本次注册页通过。
- 本地真实界面核验：桌面端注册页与登录页的品牌、双栏比例、字体、颜色、表单边框和按钮层级一致；390×844 移动端顶部与表单底部均可正常显示和滚动访问。

### 已知限制与待办

- 本次未连接真实 Identity 服务提交注册，也未验证验证码接口成功响应；仅验证了本地页面渲染、响应式布局和生产构建。
- 前端仍存在既有 TypeScript 错误与主 chunk 体积警告，本次保持视觉任务范围未扩展处理。

### Git 提交

- `style: 统一登录与注册页面视觉`（本次任务）。

## BuildingBlocks .NET 10 依赖升级

### 需求

- 将 `src/BuildingBlocks/` 的底层项目统一到当前 .NET 10 SDK，并将可安全升级的 NuGet 包更新到最高正式版本，确认仓库活动解决方案能够编译。

### 代码事实

- `src/BuildingBlocks/` 下 13 个项目和 `src/Directory.Build.props` 在本次任务开始前均已使用 `net10.0`，本次无需批量改写目标框架。
- 当前正式 .NET 10 SDK 为 `10.0.302`；原 `global.json` 仍指定 `10.0.102`、允许预览 SDK，并使用可能跨主版本的 `latestMajor`。
- `Asp.Versioning` 10.2.1 已移除旧的 `EnableApiVersionBinding()` 扩展，并通过 `Asp.Versioning.OpenApi` 与 `WithDocumentPerVersion()` 提供版本化 OpenAPI 文档。
- `Asp.Versioning.OpenApi` 10.2.1 要求 `Microsoft.OpenApi < 3.0.0`，因此不能同时使用 3.x；本次选择最高兼容的 2.x 正式版 2.11.0。
- MassTransit 9.2.0 属于商业许可版本线；项目尚未确认商业许可，故保持最高 8.x 正式版 8.5.10，避免无授权跨主版本升级。

### 实现与涉及文件

- 更新 `src/global.json`：SDK 基线改为 `10.0.302`，回滚策略改为 `latestFeature`，并设置 `allowPrerelease=false`。
- 更新 `src/Directory.Packages.props`：统一升级 EF Core、Microsoft.Extensions、ASP.NET Core、Serilog、Mapster、MediatR、Humanizer、Polly、Asp.Versioning、Scalar、Redis、MemoryPack、Minio、MongoDB 等 BuildingBlocks 直接依赖。
- 更新 `src/BuildingBlocks/BuildingBlocks.OpenApi/BuildingBlocks.OpenApi.csproj`，增加 `Asp.Versioning.OpenApi` 包引用。
- 合并 OpenAPI 与 API Versioning 注册入口，移除已删除的 `EnableApiVersionBinding()`，通过 `MapOpenApi().WithDocumentPerVersion()` 为各 API 版本生成独立文档。
- Identity、Practice、Vocabulary 三个服务改为调用统一的 `AddAspnetOpenApi()` 注册入口。

### 验证结果

- `dotnet restore src\LexiCraft.slnx --configfile src\nuget.config`：通过。
- 13 个 BuildingBlocks 项目逐一构建：全部通过，均为 0 个警告、0 个错误。
- `dotnet build src\LexiCraft.slnx --no-restore --disable-build-servers --verbosity minimal`：通过，0 个警告、0 个错误。
- BuildingBlocks 直接依赖过期检查：仅剩 MassTransit 8.5.10→9.2.0 和 Microsoft.OpenApi 2.11.0→3.9.0，两项均为已记录的许可或兼容性例外。
- BuildingBlocks 依赖漏洞检查：未发现已知漏洞；直接依赖弃用检查：未发现弃用包。
- `git diff --check`：通过。

### 已知限制与待办

- 本次以还原、逐项目构建、活动解决方案构建和 NuGet 审计为验证边界，未启动 Aspire，也未连接真实数据库、RabbitMQ、Redis、OSS 或在线检查 Scalar/OpenAPI 页面。
- 若项目后续取得 MassTransit 9 商业许可，需要单独评估并执行 8→9 主版本迁移，而不是仅修改包版本。
- 任务开始前已有 `LexiCraft.sln`、`src/LexiCraft.slnx`、`src/compose.yaml` 与 Files SQLite 运行文件修改；本次未覆盖、回滚或暂存这些改动。

### Git 提交

- `build: 升级 BuildingBlocks 的 .NET 10 依赖`（本次任务）。

## BuildingBlocks.OpenApi 使用文档

### 需求

- 为 `src/BuildingBlocks/BuildingBlocks.OpenApi/` 补全 README，说明类库职责、服务接入方式和多版本端点定义。

### 代码事实

- 类库通过 `AddAspnetOpenApi()` 注册 API Versioning、ApiExplorer、OpenAPI 和三个文档转换器。
- `UseAspnetOpenApi()` 使用 `WithDocumentPerVersion()` 映射 `/openapi/{groupName}.json`，并通过 `DescribeApiVersions()` 向 Scalar 注册版本文档。
- Identity、Practice、Vocabulary 当前仅在 Development 环境调用 `UseAspnetOpenApi()`。
- 端点版本不会自动继承；仅声明 v2 Group 而不映射端点，不会让 v1 端点自动进入 v2 文档。

### 实现与涉及文件

- 新增 `src/BuildingBlocks/BuildingBlocks.OpenApi/README.md`。
- 文档覆盖项目职责、目录结构、项目引用、`OpenApiOptions` 配置字段、服务注册、运行时映射、单版本与多版本端点、共享实现、`MapToApiVersion()`、废弃版本、文档地址、转换器行为、常见问题、依赖边界和验证命令。
- 明确记录 Bearer 转换器当前会向所有 OpenAPI 操作添加安全要求，即使实际端点使用 `AllowAnonymous()`；该行为只影响文档展示，不改变 ASP.NET Core 授权结果。

### 验证结果

- README UTF-8 无 BOM，共 402 行，Markdown 代码围栏成对。
- `dotnet build src\BuildingBlocks\BuildingBlocks.OpenApi\BuildingBlocks.OpenApi.csproj --no-restore --disable-build-servers --verbosity minimal`：通过，0 个警告、0 个错误。
- `git diff --check -- src/BuildingBlocks/BuildingBlocks.OpenApi/README.md`：通过。

### 已知限制与待办

- 本次为文档任务，未启动 Aspire 或业务服务验证 Scalar 页面和 `/openapi/v1.json`、`/openapi/v2.json` 的实际响应。
- 任务开始前已有解决方案、Compose 与 Files SQLite 运行文件修改，本次未覆盖、回滚或暂存。

### Git 提交

- `docs: 补全 OpenAPI 类库使用文档`（本次任务）。
