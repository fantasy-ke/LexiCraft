# 2026-08-07 项目盘点与治理初始化

## 需求

- 参考 `D:\myCode\Agent\FkeMark\AGENTS.md`，为 LexiCraft 建立项目规则文件。
- 建立长期记忆和按自然日分文件的每日记忆机制。
- 默认在任务完成后创建 Git 提交。
- 了解现有整个项目框架，记录当前事实，给出具体优化方案和需要与前端对接的内容。

## 本次变更

- 新增根规则文件：`AGENTS.md`。
- 新增长期记忆：`.agents/MEMORY.md`。
- 新增当天记录：`.agents/memory/2026-08-07.md`。
- 新增方案文档：`docs/方案设计.md`。
- 未修改业务代码、运行配置或前端代码。
- 规则适配了 LexiCraft 的 `.NET + Aspire + YARP + Vue` 技术栈，没有直接复制 FkeMark 的 Tauri/Markdown 规则。

## 仓库状态与入口

- 任务开始时 `git status --short` 为空，工作区干净。
- 当前主要解决方案是 `src/LexiCraft.slnx`；`dotnet sln src\\LexiCraft.slnx list` 能列出网关、基础组件、Aspire 宿主、Files、Identity、Vocabulary、Practice、Shared 和前端 `esproj`。
- 根目录 `LexiCraft.sln` 仍保留较早的项目清单，包含当前 `.slnx` 未采用的旧项目项，后续需要统一解决方案职责。
- Aspire 编排入口是 `src/LexiCraft.Aspire.Host/AppHost.cs`，注册了 Identity、Vocabulary、Practice、Files gRPC 和 API Gateway，并为业务服务绑定 PostgreSQL/MongoDB/Redis 连接资源。

## 当前后端框架

### 基础运行层

- `src/LexiCraft.Aspire.ServiceDefaults/Extensions.cs` 提供 AgileConfig 接入、健康检查、服务发现、HttpClient resilience、OpenTelemetry 指标和链路追踪。
- `src/ApiGateway/LexiCraft.ApiGateway/Program.cs` 使用 YARP、CORS、限流、安全头和默认健康端点。
- `src/BuildingBlocks/` 提供认证授权、缓存和分布式锁、EF Core/PostgreSQL、MongoDB、OSS、事件总线/MassTransit、日志、OpenAPI 和验证能力。

### 业务服务

- Identity：`src/microservices/Identity/`。包含登录、注册、验证码、刷新令牌、OAuth 初始化/回调、登出、用户资料、头像和权限端点；使用 JWT Bearer 与权限声明。
- Vocabulary：`src/microservices/Vocabulary/`。包含单词搜索、词库列表、按词库分页取词、词库导入、用户单词状态和薄弱词查询；使用 PostgreSQL 与 Redis。
- Practice：`src/microservices/Practice/`。包含创建练习任务、完成任务、提交答案；使用 MongoDB 与 Redis。
- Files：`src/microservices/Files/LexiCraft.Files.Grpc/`。提供 gRPC 文件服务、`/content?relativePath=...` 内容读取、`/uploads` 静态目录和 OSS 接入。

### API 路径事实

服务端代码定义的模块前缀为：

- Identity：`api/v{version:apiVersion}/identity`
- Vocabulary：`api/v{version:apiVersion}/vocabulary`
- Practice：`api/v{version:apiVersion}/practice`

当前已扫描到的主要端点：

| 服务 | 方法与相对路径 | 权限/说明 |
| --- | --- | --- |
| Identity | `POST login`、`POST register`、`GET users/captcha`、`POST refresh-token`、`POST logout` | 登录、注册、验证码和令牌流程；登录/注册/刷新允许匿名 |
| Identity | `GET/PUT users/info`、`POST uploadAvatar` | 用户资料和头像；需要认证/权限 |
| Identity | `GET oauth/{provider}/initiate`、`POST oauth/{provider}/callback` | OAuth 流程；回调允许匿名 |
| Identity | `GET permissions/{userId:guid}` 及权限增删改 | 权限管理 |
| Vocabulary | `GET words`、`GET word-lists`、`GET word-lists/{id}/words`、`POST word-lists/import` | 搜索、词库、分页取词和导入 |
| Vocabulary | `POST user-words/state`、`GET user-words/weak` | 掌握状态和薄弱词 |
| Practice | `POST /tasks`、`PUT /tasks/{taskId}/complete`、`POST /assessments/submit` | 练习任务和答案评估 |
| Files | `GET /content`、静态 `/uploads`、gRPC `FilesService` | 文件内容与对象存储 |

端点通常经过 `src/BuildingBlocks/BuildingBlocks/Filters/ResultEndPointFilter.cs` 包装成 `ResultDto`；`src/BuildingBlocks/BuildingBlocks/Shared/ResultDto.cs` 定义了 `Status`、`Message`、`StatusCode`、`Data` 和扩展字段。实际 JSON 字段大小写需要通过 `ConfigureJson` 或运行响应确认。

## 当前前端框架

- 工程：`src/UIs/lexicraft-vue-frontend/`。
- 构建：Vite 7、Vue 3、TypeScript、Vue Router、Pinia；样式与工程能力包括 UnoCSS、Vue Macros、自动组件/图标导入、Sass 和分析构建。
- 页面路由包含主页、登录/注册/OAuth 回调、仪表板、词汇、文章、练习、用户中心、设置、反馈和文档页面；登录后应用路由位于 `/app`，练习页使用独立布局。
- 新认证 API：`src/apis/auth.ts` + `src/utils/authHttp.ts` + `src/utils/tokenManager.ts`。
- 旧业务 API：`src/apis/user.ts`、`words.ts`、`dict.ts`、`member.ts` 和 `src/utils/http.ts`，仍能看到 `user/*`、`word/*`、`dict/*` 等旧路径。
- `src/config/env.ts` 定义了网关、Identity 和 Files 地址；`authHttp.ts` 与 `tokenManager.ts` 仍存在 `http://localhost:5001` 回退值，造成环境配置分叉。
- `package.json` 的 `test` 脚本为空，但仓库已有 Vitest 测试源码，且当前依赖/类型配置没有让 `vitest` 被 `vue-tsc` 正确解析。

## 前后端联调清单

### 已确认的契约

1. 前端新认证客户端以 `/v1/...` 作为相对路径，并在配置中把 Identity 基址设为网关的 `/identity` 前缀。
2. 后端直接服务路由包含 `/api/v1/identity/...`、`/api/v1/vocabulary/...`、`/api/v1/practice/...`；是否由 YARP 自动补充 `/api` 和服务前缀，当前仓库无法从 `appsettings.json` 证明，因为其中 `ReverseProxy` 节点为空。
3. 前端认证客户端使用 `Authorization: Bearer`；后端业务端点使用 JWT/权限授权。
4. 后端登录/注册/刷新响应 DTO 当前定义为 `Token` 与 `RefreshToken`；前端同时存在 `token`、`accessToken`、`refreshToken`、`expiresIn` 多种假设。
5. 后端注册密码校验最低 6 位且要求字母和数字；前端认证配置要求 8 至 20 位并要求大小写与数字，规则不一致。
6. 后端统一结果包络是 `ResultDto`，前端 `auth.ts` 有对应类型；旧 `http.ts` 使用另一套 `code/msg/success/data` 包络。
7. 开发环境 CORS 使用允许任意来源；非开发环境要求配置明确的允许来源，前端部署域名必须纳入配置。

### 前端当前不一致或缺口

- `src/config/env.ts`、`src/utils/authHttp.ts`、`src/utils/tokenManager.ts` 的 API 默认值不统一；认证客户端可能回退到 Identity 直连端口，而普通客户端走网关。
- 网关本地没有可审查的 YARP 路由快照，前端无法仅凭仓库确认 `/identity/v1/...` 是否转换到服务端的 `/api/v1/identity/...`。
- `auth.ts` 暴露了邮箱验证、密码重置、OAuth 提供商列表/绑定、会话查询和撤销会话等方法；在当前扫描到的 Identity Endpoint 中未发现对应实现，需要后端确认“未实现、外置或旧服务提供”。
- `src/apis/words.ts`、`dict.ts`、`user.ts` 仍使用旧业务路径；尚未看到它们与当前 Vocabulary/Practice 版本化端点的一一映射。
- 当前前端 API 目录未形成清晰的 Practice/Files 客户端契约；需要补齐练习任务、提交答案、完成任务和文件内容读取的类型化模块。
- `ResultDto` 字段大小写、空数据和错误字段需要用真实响应固定；不能同时保留两种错误归一化规则。
- OAuth 回调需要确认前端 `redirectUri`、后端 OAuth 配置、网关前缀和 CORS/安全头是否一致。

### 需要后端提供给前端的内容

1. 各环境唯一的网关公共基址，以及是否必须附加 `/api`、`/identity`、`/vocabulary`、`/practice`、`/files`。
2. YARP 路由和路径转换的脱敏配置或 OpenAPI 可访问地址；明确容器内目标服务名、超时和失败响应。
3. 每个服务的 OpenAPI/Scalar 文档地址、API 版本策略和弃用策略。
4. 统一 JSON 响应/错误契约：字段大小写、成功/失败判断、校验错误结构、追踪 ID、429 和 5xx 格式。
5. 认证契约：访问令牌字段、刷新令牌字段、过期时间单位、刷新令牌轮换、失效条件、401 行为和 OAuth 回调参数。
6. 注册和密码策略的唯一版本，前端校验与后端验证必须一致。
7. Identity 权限码清单，以及各 Vocabulary/Practice/Files 端点的权限要求。
8. 文件上传大小、内容类型、文件名/路径规则、访问 URL、鉴权和错误处理。
9. CORS 允许来源、预检头、生产域名和 OAuth 回调域名。
10. Practice 的任务创建、答案提交、完成任务请求/响应模型，以及分页、重试和幂等规则。

### 前端需要完成的内容

1. 将所有业务请求收敛到网关基址和服务域模块，删除散落的直连端口回退。
2. 将 `auth.ts` 与后端 TokenResponse/ResultDto 对齐，明确是否保留 `expiresIn`，并统一刷新并发、401 重试和登出行为。
3. 建立 Vocabulary、Practice、Files 类型化 API 模块，逐步替换旧 `user/*`、`word/*`、`dict/*` 封装。
4. 根据后端 OpenAPI 生成或维护 TypeScript DTO，禁止手工复制不一致字段。
5. 为登录、刷新令牌、权限失败、限流、文件上传和练习提交补充测试与用户反馈。
6. 配置真实可执行的 Vitest 脚本和依赖；修复当前类型检查阻塞后再把构建结果纳入 CI 门禁。
7. 与后端共同验证 OAuth 回调、CORS、路径转换和生产 CSP。

### 联调验收顺序

1. 网关健康检查和路由映射。
2. 验证码 → 注册 → 登录 → 获取用户资料。
3. 刷新令牌、并发 401 重试和登出。
4. 词库列表 → 词库分页取词 → 搜索 → 更新单词状态 → 薄弱词。
5. 创建练习任务 → 提交答案 → 完成任务。
6. 文件上传/内容读取及权限、大小、类型错误。
7. CORS、429、400/401/403/404/409/5xx 和追踪 ID。

## 当前构建基线

以下结果来自 2026-08-07，属于本次治理前的基线，不是新增规则文件造成的失败：

- `dotnet build src\\LexiCraft.slnx --no-restore` 失败，`BuildingBlocks.EventBus` 在生成的 `obj\\Debug\\net10.0` 文件中报告 8 个重复程序集属性（CS0579）。需要后续单独排查项目是否存在生成文件/旧产物冲突；本次不清理或修改业务项目。
- `npm --prefix src\\UIs\\lexicraft-vue-frontend run build-tsc` 失败，包含既有 Vue 类型错误、`QuestionForm`/通用组件类型问题、`vip.vue` DOM 类型问题，以及测试源码无法解析 `vitest`。
- `package.json` 的 `test` 脚本为空，当前不能作为测试通过证据。
- 活跃代码中已发现 7 个超过 800 行的文件：后端 `CacheService.cs` 813 行、`MinioOSSService.cs` 949 行；前端 `EditArticle.vue` 834 行、`TypingArticle.vue` 963 行、`TypeWord.vue` 803 行、`practice-words/[id].vue` 902 行、`dict-detail.vue` 1187 行；规则已将其作为技术债跟踪。

## 具体优化方案

### P0：先打通可验证联调链路

- 建立唯一网关公共基址和环境变量契约，移除前端直连 Identity 的隐式回退。
- 为 YARP 路由提供脱敏、可审查的版本化快照或 CI 导出校验，验证 `/identity`、`/vocabulary`、`/practice`、`/files` 到实际服务路由的转换。
- 统一 ResultDto、错误字段、令牌字段、过期时间和注册密码规则；新增最小登录/刷新集成测试。
- 将连接字符串、AgileConfig 参数和其他凭据迁移到环境变量/用户机密/部署密钥管理，并对已暴露凭据安排轮换。

### P1：补齐业务契约和工程门禁

- 建立按服务域划分的前端 API 模块和共享 DTO 来源，替换旧业务路径。
- 为 Vocabulary、Practice、Files 增加端到端或契约冒烟测试。
- 修复前端测试依赖和脚本，使 Vitest 能在本地和 CI 执行。
- 排查 `BuildingBlocks.EventBus` 重复程序集属性，统一根 `.sln` 与 `src/LexiCraft.slnx` 的项目清单。
- 将健康检查、限流、安全头和 CORS 纳入网关 smoke test。

### P2：降低维护和运行成本

- 拆分两个超过 800 行的基础服务文件，保持职责边界清晰。
- 明确生产环境健康探针暴露策略和 OpenTelemetry 导出目标。
- 为事件总线/跨服务流程补充幂等键、重试、补偿和事件版本文档。
- 清理 README、Docker 部署文档和实际项目清单之间的漂移，所有部署示例使用占位符。

## 验证与提交

- 规则和记忆文件写入前工作区干净。
- 本次基线命令已执行并记录失败原因；未修改源代码。
- 治理文件已完成 `git diff --check` 和内容复核。
- 治理提交：`4a50514 docs: 建立项目规则与架构记忆`。
- 该提交由初始提交 `4572fb4` amend 得到；本次记录校正作为后续独立提交保存。

## 2026-08-07 P0 ?????????

### ??

- ????? `docs/????.md` ? P0 ???
- ?????????????? `172.31.21.1`??? AgileConfig `Secret` ?????/??/OSS ??????????????????????????????????????

### ????

- `src/ApiGateway/LexiCraft.ApiGateway/appsettings.Development.json` ? `appsettings.Docker.json` ?????? YARP ???Files ??????Identity/Vocabulary/Practice ???? `v1` ?????????? `/api/{version}/...`?
- `src/ApiGateway/LexiCraft.ApiGateway/appsettings.json` ? `ReverseProxy` ??????AgileConfig/?????????????????????????
- ?? JSON ???????????`ResultDto`/`TokenResponse` ?????? PascalCase ????? `TokenResponse` ??? `expiresIn`???????????

### ???????

- ???? `VITE_API_BASE_URL` ???? `API`?`IDENTITY_API`?`FILES_API`???????? `http://localhost:5000`????????? TokenManager ? `localhost:5001` ?????
- `authHttp` ?????? PascalCase/camelCase ? `ResultDto` ? Token ???401 ?????????????????????????? 429 ?????
- `TokenManager` ????????????????????`ITokenManager` ???????
- `docs/????.md` ?? P0 ??????????????????????????????????? YARP ??????????
- `src/UIs/lexicraft-vue-frontend/src/docs/AUTH_INTEGRATION.md` ???????????

### ????

- `docs/????.md`
- `src/UIs/lexicraft-vue-frontend/src/config/auth.config.ts`
- `src/UIs/lexicraft-vue-frontend/src/config/env.ts`
- `src/UIs/lexicraft-vue-frontend/src/docs/AUTH_INTEGRATION.md`
- `src/UIs/lexicraft-vue-frontend/src/types/auth.ts`
- `src/UIs/lexicraft-vue-frontend/src/utils/authHttp.ts`
- `src/UIs/lexicraft-vue-frontend/src/utils/tokenManager.ts`

### ????

- YARP Development/Docker ?? JSON ????????? route?path transform?cluster ????????
- `npm run build` ?????????????? chunk ???
- `git diff --check` ???
- `npm run build-tsc` ?????? Vue ??????????? `vitest` ?????????????????
- `dotnet build src\LexiCraft.slnx --no-restore` ?????? `BuildingBlocks.EventBus` ??????????CS0579?8 ?????????????
- ??????Identity?Redis/???? AgileConfig???????????????????????????????????

### ??? Git

- ???????????? AgileConfig/YARP ??????????????????? P0 ??????
- ?????????????? API ???Vitest ???????????? Vocabulary/Practice/Files ?????
- Git 提交：已完成 `fix: 优化 P0 联调认证链路`，最终提交哈希以 Git 记录为准。
