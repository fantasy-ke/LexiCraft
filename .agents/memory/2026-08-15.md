# 2026-08-15 工作记录

## 测试解决方案边界调整

- 需求：将测试项目集中到独立测试解决方案，主项目解决方案不再显示测试类库。
- 代码事实：`src/LexiCraft.slnx` 原来包含 Authorization、Caching、Persistence 三个测试项目；`src/LexiCraft.Tests.slnx` 原来仅包含 Authorization、Messaging，测试清单不同步。
- 实现变更：从 `src/LexiCraft.slnx` 移除三个 `*.Tests.csproj`；将 Caching、Persistence 补入 `src/LexiCraft.Tests.slnx`，使测试解决方案只显式包含 Authorization、Caching、Messaging、Persistence 四个测试项目。测试项目引用的生产类库继续通过 `ProjectReference` 参与构建，不在测试解决方案中重复显式列出。
- 涉及文件：`src/LexiCraft.slnx`、`src/LexiCraft.Tests.slnx`、`.agents/MEMORY.md`、`.agents/memory/2026-08-15.md`。
- 验证结果：`dotnet sln src/LexiCraft.slnx list` 未发现 `*.Tests.csproj`；`dotnet sln src/LexiCraft.Tests.slnx list` 列出四个测试项目；`dotnet build src/LexiCraft.slnx --no-restore --nologo` 通过，0 警告、0 错误；`dotnet test src/LexiCraft.Tests.slnx --no-restore --nologo` 通过，Authorization 20/20、Caching 7/7、Messaging 7/7、Persistence 30/30，共 64/64。
- 已知限制：主解决方案构建期间，前端链中的子 `dotnet` 曾提示 `src/global.json` 请求 10.0.302 而当前子进程未找到兼容 SDK，但该提示未阻断本次构建，最终构建仍为 0 警告、0 错误；SDK 环境一致性不是本次任务范围。
- 待办：后续新增测试项目时只加入 `src/LexiCraft.Tests.slnx`，并以该解决方案执行全量测试。
- Git 提交信息：`build: 拆分主解决方案与测试解决方案`。
