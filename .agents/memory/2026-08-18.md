# 2026-08-18 项目现状梳理（只读）

## 需求
了解项目整体信息：技术栈、服务边界、解决方案入口、当前分支的进行中改动与风险。不修改代码。

## 代码事实
- 当前分支 `refactor/fantasy-library-docs`，与 `main` 无差异提交（`git log main..HEAD` 为空）；161 个未提交条目全部是工作区中的 `LexiCraft.* -> Fantasy.*` 重命名（10 R + 109 RM + 41 M + 1 ??）。
- 磁盘上主解决方案已改名为 `src/Fantasy.slnx`，测试解决方案为 `src/Fantasy.Tests.slnx`；`src/LexiCraft.slnx` 已不存在。
- 重命名只完成 Identity / Files / Shared / Aspire.ServiceDefaults 四条线。Vocabulary、Practice、ApiGateway、Aspire.Host 的目录、csproj、命名空间、Dockerfile 仍是 `LexiCraft.*`，`Fantasy.slnx` 中也仍按旧路径引用它们，因此当前状态自洽可构建。
- `AppHost.cs` 中 Aspire 资源名已半改：`fantasy-identity-api`、`fantasy-files-grpc` 为新名，`lexicraft-vocabulary-api`、`lexicraft-practice-api`、`lexicraft-api-gateway` 仍为旧名。
- `BuildingBlocks.Authorization/Options/PermissionAuthorizationOptions.cs:12` 的默认 `IdentityApiBaseAddress` 仍是 `https+http://lexicraft-identity-api`，与已改名的 Aspire 资源不一致；Aspire 下由 AppHost 显式注入环境变量覆盖，脱离 Aspire 的部署会解析到不存在的服务名。
- 网关 `src/ApiGateway/LexiCraft.ApiGateway/appsettings.json` 的 `ReverseProxy` 仍是空节点，真实路由来自 AgileConfig 或运行环境。
- 代码规模：449 个 `.cs`（排除 bin/obj）、127 个 `.vue`（排除 node_modules）。前端目录 `src/UIs/lexicraft-vue-frontend/` 与其 `.esproj` 未改名。
- 前端 `package.json` 的 `test` 脚本仍为空字符串。

## 验证结果
- `dotnet build src/Fantasy.slnx -v minimal`：成功，0 警告 0 错误（4.49s）。确认当前半重命名状态可构建。
- 未运行 `dotnet test`、前端构建和 Aspire 编排。

## 已知限制与待办
- 文档与命令口径未同步：`AGENTS.md`（11/13/120 行等）、`.agents/MEMORY.md`（9/10/56/86/99 行等）、`BuildingBlocks.{Authorization,Caching,OpenApi}/README.md` 仍写 `src/LexiCraft.slnx` / `src/LexiCraft.Tests.slnx`，这些命令现在会直接失败。
- `src/microservices/Files/Fantasy.Files.Grpc/LexiCraft.Files.Grpc.csproj.user`、`src/microservices/Identity/Fantasy.Services.Identity.Api/LexiCraft.Services.Identity.Api.csproj.user` 已随目录迁移但文件名未改，属遗漏。
- `src/compose.yaml` 的镜像名、服务名与 `lexicraft-net` 网络仍为旧命名；若 AgileConfig 路由按旧服务名配置，改 Aspire 资源名会断掉网关转发，需在运行环境核对后再统一。
- `MEMORY.md:22` 仍写 `LexiCraft.Shared` 与 `LexiCraftPermissionDefinitionProvider`，代码中已无该标识符。

## Git 提交
本次为只读梳理，未修改代码，未创建提交；工作区的重命名改动完整保留。

---

# 2026-08-18 基础类库文档与 Fantasy 命名迁移

## 需求

- 为持久化、缓存和权限验证基础类库补齐中文 XML 注释与 README，说明用途、结构、配置、注册、使用方式和边界。
- `src/BuildingBlocks/`、Identity、Files 的受控源码、项目文件和 README 对旧品牌做大小写不敏感零命中。
- Identity、Files、Shared、Aspire ServiceDefaults 与生产/测试解决方案改为 `Fantasy.*`；Vocabulary、Practice 保持原名。
- 运行时标识采用兼容迁移，不机械替换凭据，不丢弃历史事件、幂等记录和 OSS 对象。

## 代码事实与命名映射

- 生产/测试解决方案为 `src/Fantasy.slnx`、`src/Fantasy.Tests.slnx`。
- Identity 为 `Fantasy.Services.Identity` / `Fantasy.Services.Identity.Api`；Files 为 `Fantasy.Files.Grpc`；Shared 为 `Fantasy.Shared`；ServiceDefaults 为 `Fantasy.Aspire.ServiceDefaults`。
- Vocabulary、Practice 的目录、项目、程序集、命名空间和 Aspire 资源名保持 `LexiCraft.*`；ApiGateway、Aspire Host 与前端也保持现有名称。
- 初次实现曾把 Identity/Files Aspire 资源名与 AgileConfig AppId 一并改为 `fantasy-identity-api`、`fantasy-files-grpc`；该 AppId 处理随后按用户要求修正，见本文末尾“AppId 与 Gateway 修正”。

## 实现与文档变更

- 七个目标类库启用 XML 文档生成并补齐公共 API/关键内部流程注释：
  - `BuildingBlocks.Persistence.Abstractions`
  - `BuildingBlocks.EntityFrameworkCore`
  - `BuildingBlocks.EntityFrameworkCore.Postgres`
  - `BuildingBlocks.MongoDB`
  - `BuildingBlocks.Caching`
  - `BuildingBlocks.Authorization`
  - `BuildingBlocks.Authorization.Redis`
- 完善上述七个 README；新增 Authorization.Redis README。文档覆盖职责边界、依赖、目录、公共 API、注册/配置、最小示例、主流程、取消/异常/安全语义、限制和验证命令。
- `EventTypeResolver` 支持历史事件类型名：精确解析失败后，只在注册事件程序集内按去掉首段后的程序集名和完整类型名唯一匹配；未知或歧义候选拒绝解析并告警。
- 幂等选项新增只读 `LegacyPrefixes`；新租约和结果只写 `fantasy:idempotency` 当前前缀，历史 TTL 内记录可读取。
- Files 新增 `FilesStorageCompatibilityOptions.LegacyBucket`；OSS 读取/删除先查当前 bucket，再回退旧 bucket。历史本地 `FullPath` 仅在重新推导相对路径、匹配 `FilePath` 并通过上传根目录约束后使用。
- Identity committed JWT Secret 改为空值外部注入契约；不生成或提交替代密钥。
- 清理 BuildingBlocks 的品牌耦合：授权默认 Identity 地址、Mongo convention 名称、Serilog 模板、测试临时文件前缀及多个 README 示例。
- 更新 `AGENTS.md`、根 README、Docker 部署说明与历史设计/审查文档中的命名迁移说明。

## 验证结果

- 七个目标类库逐个 `--no-incremental` 构建：全部 0 警告、0 错误，XML 文档警告为 0。
- `dotnet build src/Fantasy.slnx -v minimal`：通过，0 警告、0 错误。
- `dotnet test src/Fantasy.Tests.slnx -v minimal`：通过，80/80，0 跳过：Authorization 20、Caching 7、Idempotency 12、Messaging 11、Persistence 30。
- `docker compose -f src/compose.yaml config --quiet`：通过。
- `docker compose build identity-api files-grpc`：通过；`fantasy.services.identity.api` 与 `fantasy.files.grpc` 的 Release build/publish 均为 0 警告、0 错误。
- Identity migrations list：3 个迁移；Files migrations list：1 个迁移。迁移 ID 保持不变。
- Identity `has-pending-model-changes` 报告存在模型变化；在 `git archive HEAD` 的改名前基线中构建并执行相同命令得到相同结果，确认是历史模型漂移，不是本次命名迁移引入。迁移文件本次只改 CLR 命名空间字符串。
- `src/BuildingBlocks`、Identity、Files 排除生成目录和 `fliesdb*` 后，旧品牌大小写不敏感扫描 0 命中。
- Files 三个 SQLite/WAL 运行文件保留为用户原有修改，不纳入任务提交。

## 已知限制与发布前置条件

- 未在真实 AgileConfig、PostgreSQL、Redis、RabbitMQ、Minio 和完整 Aspire 环境执行登录/刷新、权限、头像、文件读取与历史事件回放端到端冒烟。
- 初次实现曾要求复制新的 AgileConfig AppId 配置；该结论已被后续用户要求覆盖，最终继续复用原 AppId。仍须由环境注入当前有效 JWT Secret、同步 Collector/Dashboard/Seq/告警中的遥测名称，并核对外部 MassTransit 消费者与积压/死信。
- Files 迁移期配置旧 bucket，复制历史对象并验证覆盖后再移除 fallback；幂等旧前缀等 TTL 全部过期后移除。
- EF CLI 10.0.2 低于 runtime 10.0.10，命令输出包含工具版本提示；不影响本次 migrations list 与基线对比结论。

## Git 提交

- 提交信息：`refactor: 统一基础组件文档与 Fantasy 服务命名`；最终哈希以 `git log -1` 为准。
- 仅创建本地提交，不推送、不创建 PR。

---

# 2026-08-18 AgileConfig AppId 与 Gateway 修正

## 需求

- AgileConfig AppId 不随项目、程序集或 Aspire 资源改名，保持配置中心既有值。
- API Gateway 也迁移到 `Fantasy.*` 项目身份。

## 最终契约

- Aspire 资源名：`fantasy-identity-api`、`fantasy-files-grpc`、`fantasy-api-gateway`。
- AgileConfig AppId：`lexicraft-identity-api`、`lexicraft-files-grpc`、`lexicraft-api-gateway`。
- Gateway 项目、程序集与命名空间：`src/ApiGateway/Fantasy.ApiGateway`、`Fantasy.ApiGateway.dll`、`Fantasy.ApiGateway.*`。
- Gateway Compose 服务与镜像：`fantasy.apigateway`。
- Gateway 对外端口、YARP 路由/转换、后端 DNS、限流、安全头和 CORS 行为保持不变。

## 实现

- 恢复 Identity/Files 服务 appsettings、Aspire 注入配置和 Compose 环境变量中的旧 AppId。
- 网关目录、csproj、namespace、Dockerfile、解决方案引用、Aspire ProjectReference/生成类型/资源名、Compose 服务和镜像同步迁移为 Fantasy。
- Host `AgileConfig` 外层键跟随 `fantasy-*` 资源名，内层 `AppId` 保持旧值，明确区分资源发现身份与配置中心身份。
- 更新 AGENTS、根 README、Docker 部署说明、当前架构设计与长期记忆，删除复制新 AgileConfig AppId 的错误发布要求。

## 验证结果

- 当前工作区中网关自身使用 `BuildProjectReferences=false` 构建通过，生成 `Fantasy.ApiGateway.dll`，0 警告、0 错误。
- 当前工作区完整构建被任务开始后出现的并行未跟踪文件 `BuildingBlocks/Filters/ResultDtoEndpointExtensions.cs` 阻断（`CS1061 AddEndpointFilter`）；该文件不属于本任务，未修改或暂存。
- 在 `git archive HEAD` 的干净临时副本中仅应用本任务补丁后：`dotnet build src/Fantasy.slnx` 通过，0 警告、0 错误；`dotnet test src/Fantasy.Tests.slnx` 通过，80/80、0 跳过。
- `docker compose config --quiet` 通过；渲染结果确认 Gateway 镜像/服务为 `fantasy.apigateway`、端口仍为 `5000:8080`，三个兼容 AppId 均保持旧值。
- Development/Docker 的路由与 cluster 标识集合一致：files、identity、vocabulary、practice 四组均未改变。
- 在干净临时副本中构建 `fantasy.apigateway` 镜像通过，Release build/publish 0 警告、0 错误，入口程序集为 `Fantasy.ApiGateway.dll`。

## Git 提交

- 提交信息：`fix: 保留 AgileConfig AppId 并迁移 Fantasy 网关`；最终哈希以 `git log -1` 为准。
